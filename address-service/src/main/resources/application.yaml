server:  port: ${ADDRESS_SERVICE_PORT:8080}  servlet:    context-path: /address-service
spring:
  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:file:~/h2/address_db
    username: sa
    password:    driver-class-name: org.h2.Driver
    
        